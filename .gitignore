# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Claude Code local settings
.claude/settings.local.json

# Local credentials used by scripts/capture_fixtures.py. Never commit.
.secrets
.secrets.*

# Reverse-engineering artifacts (extracted APK, jadx output, strings dumps).
# Kept locally only — large, and derived from a third-party APK we don't
# redistribute.
_recon/
*.xapk
*.apk

# Research scripts and captured API fixtures — kept on disk for reference
# when iterating on the integration, but not part of the committed repo.
scripts/capture_fixtures.py
tests/fixtures/bsport_*.json
tests/fixtures/firebase_*.json
tests/fixtures/ENDPOINT_MAP.md
tests/fixtures/README.md

# HA custom-components build leftovers
dist/
build/
